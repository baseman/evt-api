{
  "name": "event-api",
  "version": "0.0.1",
  "private": true,
  "description": "Event Playing Swagger Project",
  "keywords": [],
  "author": "Stephen Hammond",
  "license": "Apache2",
  "directories": {
    "api": "api",
    "api_ds": "evt-ds",
    "resource_manager": "resource-manager",
    "dist": "dist/api",
    "coverage": "coverage/api"
  },
  "scripts": {
    "edit" : "swagger project edit",
    "build": "npm install -i && node -e \"require('grunt').tasks(['api-install-dependencies']);\"",
    "test": "node -e \"require('grunt').tasks(['api-validate']);\""
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-copy": "^0.8.1",
    "grunt-exec": "^0.4.6",
    "load-grunt-tasks": "^3.2.0",
    "swagger": "^0.7.5"
  }
}
